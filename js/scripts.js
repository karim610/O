function openModal(img) {
  // ... (same as before)
}

function closeModal() {
  // ... (same as before)
}

function addToCart() {
  // Implement your cart functionality here, e.g., updating a cart count, adding to local storage, or sending data to a server.
  alert("Product added to cart!");
}
